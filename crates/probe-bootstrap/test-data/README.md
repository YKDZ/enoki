# Test-only trust vectors

This directory contains only public keys, signed handoffs, archives, and signatures used to keep RSA-4096 verifier tests fast and deterministic. It contains no private key material and cannot issue another delegation or asset manifest. Production builds do not embed these vectors.
